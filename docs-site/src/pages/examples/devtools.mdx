---
title: Redux DevTools workflow
description: Use Redux DevTools to inspect committed form events and restore known local documents in the profile form from get-started.
showAskAi: false
---

import { DevToolsDemo } from "../../components/devtools-demo"

# Redux DevTools workflow

This example connects the profile form from [Get started](/get-started) to the
Redux DevTools browser extension. It uses the public `form-please/devtools`
entry point and does not add Redux to the application.

## Try the workflow

1. Open Redux DevTools in your browser.
2. Select the **Profile form** connection.
3. Change the name more than once.
4. Select an earlier event to restore its form document.
5. Select **Disconnect DevTools** to stop the integration.

<DevToolsDemo />

The form continues to work when the extension is absent or after you disconnect
the integration.

## What this example shows

- `createDevToolsMiddleware` connects one form to the extension.
- The monitor receives finalized form events and visible document state.
- Time travel restores only documents that the local integration still knows.
- DevTools failures do not stop form updates.
- `disconnect()` removes the listener without disposing the form.

## Copy the source

```tsx
// [!include ~/snippets/devtools.tsx]
```

Read the [Redux DevTools guide](/guides/devtools) for supported commands,
retention limits, and cleanup rules.
