---
title: History workflow
description: Add undo, redo, seek, journal export, pure replay, and validated import to the profile form from get-started.
showAskAi: false
---

import { HistoryDemo } from "../../components/history-demo"

# History workflow

This example adds user-facing document navigation to the profile form from
[Get started](/get-started). The form keeps its normal validation behavior while
the History feature stores committed document groups.

## Try the workflow

1. Change **Name** to `Grace Hopper`.
2. Select **Undo**.
3. Select **Redo**.
4. Select **Export**.
5. Change the name again.
6. Select **Import** to restore the exported journal position.
7. Select **Clear history** to keep only the current document.

<HistoryDemo />

The position output changes after each retained group. The form stays active
when an operation is unavailable or an imported value is invalid.

## What this example shows

- `createHistoryMiddleware` adds one History feature during form creation.
- `historyFeature.handle(form)` returns the handle for that exact form.
- `subscribe()` and `getSnapshot()` expose navigation state to React.
- `undo()`, `redo()`, and `seek()` submit live restore transactions.
- `export()` returns an immutable journal.
- `replayJournal()` calculates a document without changing the live form.
- `import()` validates untrusted journal data before it restores a document.

## Copy the source

```tsx
// [!include ~/snippets/history.tsx]
```

Read the [History and journals guide](/guides/history) for grouping, retention,
cursors, operation results, and restore limits.
