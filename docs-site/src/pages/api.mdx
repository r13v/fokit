---
title: API
description: The public Fokit entry points and React form kit APIs.
---

# API

This shell owns the canonical public API destination for `createFormKit`,
`createDefaultSlots`, `nativeControls`, `defineControl`, `useForm`,
`FormInstance`, `parseFormData`, and `ActionForm`.

